package cz.itstep.practice4;

public class Main {

    // implmentovat dle zadani + pridate metodu increment (zvysi velikost hran o 1)
    // doplnit o metody z Object tj. hasCode, toString, equals, clone
    // vytovrit instance (pole Shape --> Shape[])
    // vtvorit hlubokou kopii pole (napoveda kopie pole + jednotlivych prvku, prip. zda nelze pouzit knihovni funkce)
    // overit zda jsou kopie validni tj. na poli kopii zavolat metodu incremet
    // obe pole strojove porovnat (a[0].equals(c[0]) + a[0].hashCode() == c[0] atd..) a vypsat vysledek jednotlivych porovnani
    // obe pole vypsat (napoveda toString, prip. lepsi implementace pomoci vhodne knihovni funkce)

}
